# Періодичні звіти - Швидкий старт

## 🚀 Для користувачів

### Як створити та заповнити звіт за 5 кроків:

1. **Створіть звіт**
   ```
   Menu → Orange Inventory → Periodic Report → New
   Виберіть: Організація, Період (2025-Q1), Тип звіту
   Збережіть
   ```

2. **Завантажте шаблон**
   ```
   Дії → Завантажити шаблон Excel
   ```

3. **Заповніть файл**
   ```
   Відкрийте Excel → Заповніть колонку G → Збережіть
   ```

4. **Прикріпіть файл**
   ```
   Поле "Заповнена форма" → Прикріпити → Виберіть файл → Збережіть
   ```

5. **Імпортуйте дані**
   ```
   Дії → Імпортувати дані → Підтвердіть
   ```

✅ Готово!

---

## 🔄 Виправлення помилок (Ре-імпорт)

### Якщо знайшли помилку після імпорту:

1. **Виправте Excel файл**
2. **Прикріпіть виправлений файл** (замінивши старий)
3. **Ре-імпорт:**
   ```
   Дії → Ре-імпорт даних → Підтвердіть
   ```

💡 Історія попереднього імпорту зберігається в Timeline!

---

## 👨‍💼 Для адміністраторів

### Масове створення звітів:

```
Список oiPeriodicReport → Меню → "Масове створення звітів"
Період: 2025-Q2
Натисніть "Вибрати всі організації" (або виберіть конкретні)
→ Створити звіти

💡 Показуються тільки організації з показниками в oiHromadaSurvey
```

### Перевірити прострочені звіти:

```
Список oiPeriodicReport → Меню → "Прострочені звіти"
```

### Відправити нагадування:

```
Виберіть звіти (чекбокси) → Меню → "Відправити нагадування"
```

---

## 💻 Для розробників

### Створити звіт через API:

```python
import frappe

report = frappe.get_doc({
    "doctype": "oiPeriodicReport",
    "organization": "ORG-00001",
    "period": "2025-Q2",
    "report_type": "Квартальний",
    "submission_deadline": "2025-07-15"
})
report.insert()
```

### Масове створення:

```python
result = frappe.call(
    "orange_inventory.orange_inventory.doctype.oiperiodicreport.oiperiodicreport.bulk_create_reports",
    organizations=["ORG-00001", "ORG-00002"],
    period="2025-Q2",
    report_type="Квартальний"
)
```

### Автоматизація (scheduler):

```python
# hooks.py
scheduler_events = {
    "cron": {
        "0 9 1 1,4,7,10 *": [
            "path.to.create_quarterly_reports"
        ]
    }
}
```

---

## 📊 Статуси

| Статус | Що робити |
|--------|-----------|
| 🔴 Прострочено | Терміново заповнити! |
| 🟠 Очікує заповнення | Завантажте шаблон, заповніть |
| 🔵 Заповнено | Імпортуйте дані |
| 🟢 Імпортовано | Звіт готовий |

---

## ⚠️ Важливо

- ❌ НЕ змінюйте структуру Excel файлу
- ❌ НЕ змінюйте ID показників (колонка B)
- ✅ Редагуйте ТІЛЬКИ колонку G
- ✅ Зберігайте формат періоду: `РРРР-Q1` або `РРРР`

---

## 🔗 Детальна документація

[PERIODIC_REPORTS_GUIDE.md](PERIODIC_REPORTS_GUIDE.md)
